services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: wppsender
      POSTGRES_USER: wppsender
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports: ["127.0.0.1:5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
